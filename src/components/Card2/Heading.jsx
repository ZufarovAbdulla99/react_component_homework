function Heading({headingText}) {
    return (
        <>
            <h2 style={{fontSize: "30px", fontWeight: "500", fontFamily: "monospace", margin: 0}}>{headingText}</h2>
        </>
    )
}

export default Heading